# Day 002 Challenge

Upgrade the FastAPI Task API to use PostgreSQL instead of in-memory storage.

## Objectives

- PostgreSQL database integration
- SQLAlchemy ORM models
- Alembic migrations
- Database session management
- Environment configuration

## Endpoints

POST /tasks
GET /tasks
GET /tasks/{id}
PUT /tasks/{id}
PATCH /tasks/{id}/complete
DELETE /tasks/{id}